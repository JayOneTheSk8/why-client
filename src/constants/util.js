const limits = {
  USERNAME_LIMIT: 50,
  POST_TEXT_LIMIT: 200,
  MOBILE_VIEW_PIXEL_LIMIT: 985,
};

const tokens = {
  DARK_MODE_ACTIVE: 'whyDarkModeActivated',
  CURRENT_USER: 'currentUser',
};

export default {
  limits,
  tokens,
};
