const componentMessages = {
  errorFormat: (errors) => `ERRORS: ${errors}`,
};

const keys = {
  GENERAL_ERROR: 'generalError',
  USERNAME_NOT_FOUND_ERROR: 'usernameNotFound',
};

export default {
  componentMessages,
  keys,
};
